public interface AccessInterface {
    void addItem();
    void editItem();
    void removeItem();
    //needs to be able to searchBy various things but no specific ones that will carry across types
}
